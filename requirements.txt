# Core pipeline dependencies — install via install.sh (handles PyTorch + spconv
# CUDA variants automatically) or manually for CPU-only / non-KITTI use.
numpy>=2
scipy==1.14.1
filterpy
opencv-python
PyYAML
easydict
tqdm

# Visualization
rerun-sdk[notebook]==0.33.1

# Evaluation
motmetrics

# Dev
pytest
ruff
